name: Create a Release with signed SLSA Provenance

on: push

jobs:
  build:
    name: Build artifact
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Generate provenance
      uses: slsa-framework/github-actions-demo@v0.1
      with:
        artifact_path: target
        output_path: target/build.provenance

    - name: Push provenance to remote
      env:
        TOKEN: ${{ secrets.PAT }}
      run: |
        git clone https://siberov:$TOKENgithub.com/siberov/action-store
        cd action-store
        git config --unset-all http.https://github.com/.extraheader
        git config user.name "provenance-bot"
        git config user.email "dummy@email.com"
        cp target/build.provenance .
        git add build.provenance
        git commit -m "$COMMIT"
        git push

#    - name: Create Release
#      uses: svenstaro/upload-release-action@v2
#      with:
#        repo_token: ${{ secrets.GITHUB_TOKEN }}
#        file: target/*
#        file_glob: true
#        tag: ${{ github.ref }}
#        overwrite: true
